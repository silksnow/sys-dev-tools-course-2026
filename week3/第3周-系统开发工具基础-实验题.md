---
tags:
  - 系统开发工具基础
  - Packaging-and-Shipping-Code
  - 智能体编程
  - 不止于代码
  - Python与PyTorch
课程: 系统开发工具基础
周次: 第3周
---

# 第3周 实验题（Packaging and Shipping Code / 智能体编程 / 不止于代码 / Python 与 PyTorch）

## 目录

- [[#第9题 从源码构建并在干净环境安装 Wheel]]
- [[#第10题 让编程智能体进入可验证的修复循环]]
- [[#第11题 把“无法处理”的协作材料改成可执行信息]]
- [[#第12题 修复一个可复现的线性回归训练循环]]
- [[#课后练习（missing-semester 对应章节）]]
  - [[#A. 代码打包与发布（5 题）]]
  - [[#B. 智能体编程（4 题）]]
  - [[#C. 不止于代码（3 题）]]
- [[#附录：知识点速查（第3周）]]
- [[#常用命令速查]]
- [[#附录：Python 打包与 PyTorch 速查]]

> [!note] 环境前提
> 本周 4 题覆盖四类环境，缺一不可：
> - **Q9（打包发布）**：Python 3.13 + `pip` + `build` 工具 + `venv`（标准库自带）。
>   - 装 build：`pip install build`（PEP 517 构建前端，`python -m build` 用）。
> - **Q10（智能体编程）**：在 Q9 基础上复用 `greetlab` 包，另需 `pytest`（第2周已装，自查 `pytest --version`）+ 一个编程智能体（AI 助手）。
> - **Q11（不止于代码）**：纯写作题，无额外依赖，产出一个 `communication.md`。
> - **Q12（PyTorch）**：需要 PyTorch，CPU 版即可：`pip install torch`（自查 `python -c "import torch; print(torch.__version__)"`）。
> - 下文命令跨平台（Linux / macOS / Git Bash / WSL），差异处已分别标注；`<学号>` 请替换成自己的学号，例如 `25020007111`。

---

## 第9题 从源码构建并在干净环境安装 Wheel

**主题**：Packaging and Shipping Code

> [!abstract]- 知识归档
> **Python 包的结构（src layout）**
> - `src/greetlab/`：源码放 `src/` 下，包名是 `greetlab`，模块是 `cli.py`。src 布局能把"源码"和"项目根目录"隔离开，避免误 import 未构建的半成品。
> - `__init__.py`：标记该目录是**包**（package）。内容可以留空，但没有它（旧式）目录只是普通文件夹，无法被 `import greetlab` 识别。
>
> **pyproject.toml**
> - 项目元数据 + 构建配置的**单一事实来源**（现代标准，替代旧 `setup.py`）。
> - `[build-system]`：声明构建后端，`build-backend = "setuptools.build_meta"` 表示用 setuptools 打包；`requires` 是构建时需要的依赖。
> - `[project]`：包名 `name`、版本 `version`。
> - `[project.scripts]`：**入口点（entry point）**，`sdt-greet = "greetlab.cli:main"` 表示"生成一个叫 `sdt-greet` 的命令，运行时调用 `greetlab.cli` 模块里的 `main` 函数"。
>
> **argparse**
> - Python 标准库的命令行参数解析器。`add_argument("--name", required=True)` 声明必填的 `--name` 参数；`parse_args()` 解析 `sys.argv` 并返回命名空间对象 `a`，用 `a.name` 取值。
> - 参数缺失/非法时，argparse 会打印 usage 并 `exit(2)`（注意：这是第10题的考点）。
>
> **wheel 与构建**
> - **wheel**（`.whl`）是 Python 的二进制分发格式，本质是个 zip，解压即安装，不用现场编译。
> - `python -m build` 是 PEP 517 构建前端：在隔离环境里按 `pyproject.toml` 构建，产出 `dist/` 下的 `.whl` 和 `.tar.gz`（sdist）。
>
> **虚拟环境 venv**
> - `python -m venv .venv`：创建一个**独立隔离**的 Python 环境（自带解释器 + pip），与系统 Python 互不影响。
> - 激活后 `python`、`pip`、装好的 `sdt-greet` 都来自该 venv，卸载时删目录即可。

### 第1步：建目录、写三个文件

```bash
mkdir q09 && cd q09

# 建包目录（-p 递归建，src/greetlab 一次到位）
mkdir -p src/greetlab
```

```python
# src/greetlab/__init__.py —— 留空即可，作用是标记 greetlab 是一个包
```

```python
# src/greetlab/cli.py
import argparse

def main():
    p = argparse.ArgumentParser()          # 解析器
    p.add_argument("--name", required=True)  # 必填 --name 参数
    a = p.parse_args()                     # 解析命令行，返回命名空间
    print(f"Hello, {a.name}!")             # f-string 插值输出
```

```toml
# pyproject.toml —— 把 name 里的 <学号> 换成自己的学号
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "greetlab-<学号>"
version = "0.1.0"

[project.scripts]
sdt-greet = "greetlab.cli:main"
```

> [!note] `[project.scripts]` 的 `"greetlab.cli:main"`
> 冒号左边是模块路径 `greetlab.cli`，右边是函数名 `main`。安装后 pip 会生成一个可执行脚本 `sdt-greet`，其内容等价于 `import sys; from greetlab.cli import main; main()`。

### 第2步：用 build 工具生成 wheel

```bash
# 确认已装 build（PEP 517 前端），没有就装
pip install build

# 在 q09 根目录（含 pyproject.toml 的位置）执行，产出 dist/
python -m build
```

```text
# 期望输出里出现（名字里 <学号> 换成你自己的，连字符会转下划线）：
# Successfully built greetlab_<学号>-0.1.0.tar.gz 和 greetlab_<学号>-0.1.0-py3-none-any.whl
```

```bash
# 查看产物：dist/ 下应有一个 .whl 和一个 .tar.gz
ls dist
```

> [!tip] wheel 文件名的含义
> `greetlab_<学号>-0.1.0-py3-none-any.whl` 五段：包名-版本-解释器(py3)-ABI(none)-平台(any)。
> `py3-none-any` = 纯 Python、与 ABI 无关、任意平台，说明这是**纯 Python 包**，任何机器都能装。

### 第3步：新建干净虚拟环境，只从 wheel 安装

```bash
# 在 q09 里建 venv（只装 wheel，不用 -e，不碰源码目录，保证"干净"）
python -m venv .venv

# 激活（Linux / macOS / Git Bash）：
source .venv/bin/activate
# Windows PowerShell / cmd 用：
# .venv\Scripts\activate

# 激活后提示符前出现 (.venv)，此时 python/pip 都是 venv 里的
which python         # 应指向 .venv 内的 python

# 只从 wheel 安装（不装源码，不装 sdist）
pip install dist/greetlab_<学号>-0.1.0-py3-none-any.whl
```

### 第4步：切到 q09 之外运行 sdt-greet

```bash
# 关键：离开 q09 目录（证明命令是全局安装的入口点，而非凑巧读到了本地源码）
cd ..

# 运行（仍在激活的 venv 中），<学号> 换成自己的
sdt-greet --name <学号>
# 期望输出：Hello, <学号>!

# 用 which 看这个命令来自 venv（而非系统）
which sdt-greet     # 应指向 .venv/bin/sdt-greet
```

> [!tip] 为什么强调"q09 之外运行"
> 如果还站在 q09 目录里，`python` 可能会把当前目录加入 `sys.path`，即使没装好也可能 import 到源码，掩盖问题。
> 走到目录外再跑，才真正验证"wheel 已正确安装、入口点脚本已生成"。

### 有多少种做法

**构建的两种方式**：

| 方式 | 命令 | 说明 |
| --- | --- | --- |
| PEP 517 前端（推荐） | `python -m build` | 按 `pyproject.toml` 隔离构建，产出 wheel + sdist，题目指定 |
| setuptools 直调 | `python -m pip wheel .` 或 `python setup.py bdist_wheel` | 旧式，依赖 `setup.py`，不推荐 |

**装 wheel 的两种方式**：

```bash
# 方式 A：直接指定 wheel 文件（题目要求，最严格）
pip install dist/greetlab_<学号>-0.1.0-py3-none-any.whl

# 方式 B：让 pip 从 dist 里自己挑 wheel
pip install dist/*.whl
```

---

## 第10题 让编程智能体进入可验证的修复循环

**主题**：智能体编程

> [!abstract]- 知识归档
> **测试驱动（TDD）**
> - 先写一个**会失败**的测试描述"正确行为"，再改实现让它通过。好处：目标明确、可自动验证。
> - `pytest.raises(SystemExit) as e`：断言某段代码会抛 `SystemExit`；`e.value.code` 取退出码。
>
> **为什么空白 name 应 SystemExit(2)**
> - argparse 对"参数错误"约定以退出码 `2` 结束（`argparse` 的 `error()` 就是 `exit(2)`）。
> - 空姓名是**非法输入**，理应走 error 路径退出 2，而不是当作合法输入输出 `Hello,   !` 并退出 0。
> - `p.error("消息")`：打印 usage + 错误消息到 stderr，然后以退出码 2 结束。
>
> **智能体编程的可验证循环**
> - 给智能体三点：① 目标（改什么行为）；② 约束（不许动什么）；③ 验证命令（怎么算对）。
> - 人最后要 `git diff` 审改动、撤销无关修改、再跑测试——**智能体是加速器，人负责把关**。

### 第1步：复制 q09 为 q10，加一个失败测试

```bash
# 复制整个 q09 目录为 q10（含源码与 pyproject.toml）
cp -r q09 q10
cd q10
```

```python
# test_cli.py —— 先写测试（此刻实现还没改，这个测试应当"失败"）
import sys
import pytest
from greetlab.cli import main

def test_blank_name_exits_2():
    # 传入只含空白字符的 name，main 应以退出码 2 结束
    with pytest.raises(SystemExit) as excinfo:
        sys.argv = ["sdt-greet", "--name", "   "]   # 把 argv 换成测试参数
        main()
    assert excinfo.value.code == 2
```

> [!note] 让 pytest 能 import 到 `greetlab.cli`
> 源码在 `src/` 下，测试文件在项目根。两种办法任选：
> ```bash
> # 方式 A：临时把 src 加进模块搜索路径（最省事，不装包）
> PYTHONPATH=src pytest
> # 方式 B：可编辑安装（pip install -e .），把包"链接"进环境
> python -m pip install -e . && pytest
> ```

### 第2步：先跑测试，确认失败

```bash
PYTHONPATH=src pytest
# 期望：FAILED —— test_blank_name_exits_2
# 因为当前 main 对空白 name 直接 print 并正常返回，不会抛 SystemExit(2)
```

### 第3步：把目标、约束、测试命令交给智能体

给智能体一段清晰的提示（prompt）：

```text
目标：修改 q10/src/greetlab/cli.py 的 main，使 --name 只含空白字符时以 SystemExit(2) 结束。
约束：不要改动 test_cli.py 和 pyproject.toml；不要删除 --name 参数。
验证命令：cd q10 && PYTHONPATH=src pytest
请修改实现并运行上面的测试命令，直到通过。
```

智能体应给出最小修复（在 `parse_args` 后校验 `strip` 是否为空）：

```python
# src/greetlab/cli.py（修复后）
import argparse

def main():
    p = argparse.ArgumentParser()
    p.add_argument("--name", required=True)
    a = p.parse_args()
    if not a.name.strip():          # 只含空白（或空串）判为非法
        p.error("--name 不能只含空白字符")   # 打印 usage + 消息，exit(2)
    print(f"Hello, {a.name}!")
```

> [!tip] 为什么是 `p.error` 而不是 `sys.exit(2)`
> `p.error` 会先打印 usage 与错误消息到 stderr，再 `exit(2)`，更符合 argparse 的报错习惯；`sys.exit(2)` 也能退出 2，但没有报错提示。

### 第4步：人工检查 diff、撤销无关修改、复跑测试

```bash
# 查看智能体到底改了哪些文件
git diff            # 若 q10 不是 git 仓库，可先 git init && git add -A 建个快照再 diff
# 也看每个文件具体改了什么：
git diff src/greetlab/cli.py

# 若智能体顺手改了无关文件（如改了 test、删了空行、改了 pyproject），撤销它：
git checkout -- <无关文件>     # 丢弃该文件的所有改动

# 最终确认只剩 cli.py 一处改动，再复跑测试
PYTHONPATH=src pytest
# 期望：1 passed
```

### 第5步：写 ai_log.md（不超过 5 行）

```markdown
# ai_log.md
核心提示：修改 greetlab/cli.py，使 --name 只含空白时 main() 以 SystemExit(2) 结束；测试命令 `PYTHONPATH=src pytest`，禁改测试与 pyproject。
智能体改动：parse_args 后新增 `if not a.name.strip(): p.error(...)` 分支。
人工验证：git diff 确认仅 cli.py 改动；撤销无关修改后 pytest 1 passed。
```

> [!warning] 人不当甩手掌柜
> 智能体可能"过拟合"测试：比如直接 `sys.exit(2)` 硬编码、或顺带改了测试让它"假通过"。所以必须人工 `git diff` 审改动、撤销无关修改、复跑测试——这是题目要求的"可验证修复循环"闭环。

### 有多少种做法

**让空白 name 退出 2 的几种实现**：

```python
# 方式 A：p.error（推荐，自动打印 usage + 消息）
if not a.name.strip():
    p.error("--name 不能只含空白字符")

# 方式 B：显式 sys.exit(2)（等价退出码，但无报错提示）
import sys
if not a.name.strip():
    sys.exit(2)

# 方式 C：自定义 type 校验（argparse 里写个校验函数，更"正规"但啰嗦）
def nonblank(s):
    if not s.strip():
        raise argparse.ArgumentTypeError("不能只含空白")
    return s
p.add_argument("--name", type=nonblank, required=True)
```

---

## 第11题 把“无法处理”的协作材料改成可执行信息

**主题**：不止于代码

> [!abstract]- 知识归档
> **好的 Issue**
> - 标题：一句话说清问题（不是"运行不了"）。
> - 正文四要素：**环境**、**复现步骤/命令**、**期望结果**、**实际结果**；不知道的写"待确认"。
> - 让任何一个陌生人都能照着复现，而不是只能靠猜。
>
> **好的提交信息**
> - 标题用**祈使语气**（"Fix ... / Add ... / Validate ..."），像下达命令，而不是"fix bug"。
> - 正文分两段：为什么（问题）+ 怎么做（解决方案）。
>
> **好的评审意见**
> - 指出**具体行为**（哪段、什么现象）+ **风险** + **建议动作**，并标注严重级别：
>   - `Blocking`：不修不能合入；
>   - `Suggestion`：建议改进，不阻塞；
>   - `Nit`：吹毛求疵的细节。
>
> **共同原则**：可执行（别人能据此行动）、专业、简洁、不含情绪。

### 第1步：新建 communication.md 并改写三段材料

已知事实：运行 `sdt-greet --name " "` 仍输出 `Hello,  !`，并以 0 退出（即空白姓名未校验）。

```markdown
# communication.md

## Issue（重写）
标题：sdt-greet 对空白 --name 仍输出问候并以 0 退出

环境：Linux / Python 3.13（Windows 是否可复现待确认）

复现：
    sdt-greet --name " "

期望结果：以非零退出码结束，不输出问候语。
实际结果：输出 "Hello,  !" 并以退出码 0 结束。

## 提交信息（重写）
标题：Validate --name to reject blank names

正文：--name 只含空白时仍输出问候并以 0 退出，调用方无法判错。
     在 parse_args 后校验 a.name.strip()，为空则 p.error(...) 以退出码 2 结束。

## 评审意见（重写）
[Blocking] 新增校验缺少对应测试，需补空白与空串两个用例。
[Nit] 错误消息建议统一格式，便于断言。
[Suggestion] 可复用 argparse type 校验，逻辑更内聚。
```

> [!note] 改动对照
> - Issue 从"运行不了，尽快修复"→ 补上环境、复现命令、期望/实际、待确认项。
> - 提交信息从"fix bug"→ 祈使语气标题 + 为什么/怎么做两段。
> - 评审意见从"写得不好，重写"→ 具体行为 + 风险 + 建议动作 + Blocking/Suggestion/Nit 分级。

### 第2步：自查字数与专业度

```bash
# 简单估算字数（不含空白行/代码分隔）
grep -c . communication.md
```

- 全文应控制在 **400 字以内**，只讲事实、不讲情绪、不写"尽快/不好"这类模糊词。
- 每个动作都要"可执行"：Issue 能照着复现，评审意见能照着改，提交信息能看出改了啥。

> [!tip] 三个"反例 → 正例"的精髓
> - 反例只说结果（"运行不了"），正例给出**因果链**（什么环境、什么命令、看到什么、本该什么）。
> - 反例给结论（"重写"），正例给**依据 + 动作**（哪段有问题、为什么、怎么改、多严重）。
> - 反例用祈使但不说明（"fix bug"），正例用祈使且**标题即摘要**。

---

## 第12题 修复一个可复现的线性回归训练循环

**主题**：Python 与 PyTorch

> [!abstract]- 知识归档
> **PyTorch 基础**
> - `torch.tensor`：张量（多维数组），带自动求导。`torch.linspace(-1,1,101)` 在 $[-1,1]$ 间等距取 101 个点，`.reshape(-1,1)` 转成 101×1 列向量。
> - `nn.Linear(1,1)`：全连接层，本质是 $y = Wx + b$，$W$ 是 `weight`、$b$ 是 `bias`，初始随机。
> - `nn.MSELoss`：均方误差，衡量预测与目标的差距。
> - `torch.optim.SGD(model.parameters(), lr=0.1)`：随机梯度下降优化器，`lr` 学习率。
>
> **训练三步（每次迭代都要做）**
> - `opt.zero_grad()`：清空上次累积的梯度——**PyTorch 梯度默认累加**，不清会越加越错。
> - `loss.backward()`：反向传播，沿计算图自动求每个参数的梯度。
> - `opt.step()`：按梯度更新一次参数。
>
> **评估模式与 no_grad**
> - `model.eval()`：切到评估模式（关掉 dropout / batch-norm 等训练专用行为）。
> - `torch.no_grad()`：不记录梯度、不建计算图，评估时省内存、防意外更新。
>
> **本题本质**
> - 数据由 `y = 3x - 1` 生成，所以训练收敛后 `weight ≈ 3`、`bias ≈ -1`。
> - 不能直接赋值 `weight=3; bias=-1`，必须靠训练循环**学**出来。

### 第1步：保存并补全 train.py

```python
# q12/train.py
import torch
from torch import nn

torch.manual_seed(20260907)              # 固定随机种子，保证可复现
x = torch.linspace(-1, 1, 101).reshape(-1, 1)   # 101 个输入点
y = 3 * x - 1                            # 目标：y = 3x - 1

model = nn.Linear(1, 1)                  # 线性模型 y = Wx + b
loss_fn = nn.MSELoss()                   # 均方误差
opt = torch.optim.SGD(model.parameters(), lr=0.1)

for _ in range(200):                     # 训练 200 轮
    pred = model(x)                      # 前向：算出预测
    loss = loss_fn(pred, y)              # 算损失
    opt.zero_grad()                      # TODO①：清空梯度（不清会累加）
    loss.backward()                      # TODO②：反向传播求梯度
    opt.step()                           # TODO③：按梯度更新参数

model.eval()                             # 切到评估模式
with torch.no_grad():                    # 不计算梯度
    pred = model(x)
    final_loss = loss_fn(pred, y)        # 最终损失
print(f"final loss = {final_loss.item():.6f}")
print(f"weight = {model.weight.item():.6f}")
print(f"bias   = {model.bias.item():.6f}")
```

### 第2步：运行并核对

```bash
python q12/train.py
# 期望输出（数值会有极小差异）：
# final loss = 0.000123     <- 必须小于 0.001
# weight = 2.9998...
# bias   = -0.9999...
```

> [!tip] 判断是否成功
> - `final loss < 0.001`：训练收敛（题目硬性要求）。
> - `weight ≈ 3`、`bias ≈ -1`：因为数据就是 `y=3x-1` 生成的，说明模型学到了正确关系。
> - 若 loss 很大或 weight 离谱，先查三步是否缺了 `zero_grad`（梯度累加是最常见翻车点）。

### 有多少种做法

**打印 weight / bias 的两种写法**：

```python
# 方式 A：.item() 转成 Python 标量（推荐，可配合 f-string 格式化）
print(f"weight = {model.weight.item():.6f}")

# 方式 B：直接打印 tensor（会带 grad_fn / device 信息，可读性差）
print("weight =", model.weight)
```

**训练三步的另一种顺序提醒**：`zero_grad` 一定要在 `backward` 之前，`step` 一定要在 `backward` 之后，顺序不能乱。

---

## 课后练习（missing-semester 对应章节）

> [!note] 来源
> - Packaging and Shipping Code：<https://missing-semester-cn.github.io/2026/shipping-code/>（1~5 题）
> - 智能体编程：<https://missing-semester-cn.github.io/2026/agentic-coding/>（2~5 题）
> - 不止于代码：<https://missing-semester-cn.github.io/2026/beyond-code/>（1、2、4 题）
> - 关联（正则练习出处）：代码质量 <https://missing-semester-cn.github.io/2026/code-quality/>

---

### A. 代码打包与发布（5 题）

#### A1. printenv 保存环境 → 建 venv → diff 前后差异

> [!abstract]- 知识归档
> - `printenv`：打印当前进程的所有环境变量（一行 `键=值`）；`env` 等价（还能改环境跑命令）。
> - `diff a b`：逐行比较两个文件，输出差异（`<` 是左边文件、`>` 是右边文件）。
> - 激活 venv 后环境变了：新增 `VIRTUAL_ENV`、`PATH` 开头被插入 `.venv/bin`、`PS1`（提示符）加前缀。
> - **为什么 shell 优先用 venv**：查找命令是按 `PATH` 从前往后找，venv 的 bin 排在最前，所以 `python`、`pip` 命中的是 venv 里的。
> - `deactivate` 不是一个可执行文件，而是 `activate` 脚本里定义的 **bash 函数**，负责把 `PATH`、`VIRTUAL_ENV` 等恢复原状。

```bash
printenv > before.txt          # 先把当前环境快照存下来
python -m venv .venv           # 建 venv
source .venv/bin/activate      # 激活（Win：.venv\Scripts\activate）
printenv > after.txt           # 再存一份
diff before.txt after.txt      # 看差异

# 关键差异在 PATH（先看激活前，再看激活后）
echo "$PATH"

# deactivate 是函数，不是二进制
which deactivate               # 无输出（找不到这个可执行文件）
type deactivate                # 输出：deactivate 是一个函数
deactivate                     # 退出 venv，再 echo $PATH 看是否还原
```

> [!tip] 期望的 diff 结果
> - `>` 新增：`VIRTUAL_ENV=/path/.venv`、`PS1` 变了。
> - `PATH` 那行：右边（after）开头多出 `/path/.venv/bin:`。
> - 如果 `diff` 没输出 PATH 差异，多半是你用 `echo $PATH` 看的时机不对（要在激活后、同一 shell 里看）。

#### A2. 建包 + 生成并检查 lockfile

复用第9题的 `greetlab` 包（或新建一个 `greeting` 包），装进 venv 后生成 **lockfile**。

```bash
cd q09
python -m venv .venv && source .venv/bin/activate

# 方式 A（推荐）：uv lock 生成 uv.lock（先 pip install uv）
pip install uv
uv lock                    # 解析全部依赖并写死版本，产出 uv.lock
head -20 uv.lock           # 检查：每个包有 name/version/hash

# 方式 B：pip freeze 把当前环境快照成 requirements.txt（最朴素的 lockfile）
pip freeze > requirements.txt
cat requirements.txt
```

> [!tip] lockfile 是什么、为什么重要
> lockfile（`uv.lock` / `requirements.txt` 全量版）把**每一层依赖的精确版本 + 哈希**写死，保证任何人装到的依赖完全一致（可复现）。
> `pyproject.toml` 里通常只写范围（如 `numpy>=1.24`），lockfile 才是"锁定到底装哪个版本"的那把锁。

#### A3. 装 Docker，用 docker compose 本地构建 Missing Semester 网站

```bash
# Fedora 安装（本机是 Fedora 44）
sudo dnf install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
# 或发行版自带仓库（版本稍旧）：sudo dnf install docker docker-compose

sudo systemctl enable --now docker          # 启动并开机自启
sudo usermod -aG docker "$USER"             # 加进 docker 组（重新登录生效，免 sudo）

# 拉取 missing-semester 官网仓库，里面有 docker-compose.yml
git clone https://github.com/missing-semester/missing-semester.git
cd missing-semester
docker compose up                           # 构建 Jekyll 站点并起服务
# 浏览器打开 http://localhost:4000 看效果
```

> [!tip] 概念：image vs container、Docker vs compose
> - **image**（镜像）是打包好的"模板"，**container**（容器）是镜像的一个运行实例，一个镜像可跑多个容器。
> - `docker run` 管单个容器；`docker compose` 用一份 YAML 把多个服务一起编排、起停、连网。

#### A4. 写 Dockerfile + docker-compose.yml（应用 + Redis 缓存）

一个访问计数小应用：Flask 记次数、Redis 存次数。

```python
# app.py
import os
from flask import Flask
import redis

app = Flask(__name__)
r = redis.Redis(host=os.environ.get("REDIS_HOST", "localhost"),
                port=6379, decode_responses=True)

@app.route("/")
def home():
    return f"访问次数: {r.incr('hits')}"

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5000)
```

```text
# requirements.txt
flask
redis
```

```dockerfile
# Dockerfile
FROM python:3.12-slim                 # 用 slim 轻量镜像，而非完整版
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt   # 装依赖（清理缓存减小体积）
COPY app.py .
CMD ["python", "app.py"]              # 容器启动时跑应用
```

```yaml
# docker-compose.yml
services:
  web:
    build: .                          # 用当前目录 Dockerfile 构建
    ports:
      - "5000:5000"
    environment:
      - REDIS_HOST=cache              # 服务名就是内网 DNS 主机名
    depends_on:
      - cache
  cache:
    image: redis:7-alpine             # 直接拉官方 Redis 镜像
    volumes:
      - redis_data:/data

volumes:
  redis_data:
```

```bash
docker compose up --build             # 构建并起两个容器
curl localhost:5000                   # 反复 curl，计数递增
```

> [!tip] 为什么用 `cache` 这个主机名
> docker compose 会为每个服务建内部 DNS，服务名 `cache` 自动解析到 Redis 容器 IP。web 进程不用知道 IP，写服务名即可——这是"微服务靠网络通信"的典型做法。

#### A5. 发布到 TestPyPI + 打 Docker 镜像推到 ghcr.io

```bash
# 1) 上传 TestPyPI（别传真 PyPI）
pip install build twine
python -m build                        # 产出 dist/*.whl 与 *.tar.gz

# ~/.pypirc 里配 TestPyPI 的 token（用户名 __token__，密码是 TestPyPI 生成的 API token）
twine upload -r testpypi dist/*

# 2) 从 TestPyPI 装回来验证
pip install --index-url https://test.pypi.org/simple/ greetlab-<学号>

# 3) 打 Docker 镜像（镜像里只装"你自己的包"）
```

```dockerfile
# Dockerfile
FROM python:3.12-slim
RUN pip install --index-url https://test.pypi.org/simple/ greetlab-<学号>
ENTRYPOINT ["sdt-greet"]               # 直接把它当命令行工具
```

```bash
docker build -t ghcr.io/<你的用户名>/greetlab:v0.1.0 .
docker login ghcr.io -u <你的用户名>   # 用 GitHub PAT（有 read:packages/write:packages 权限）
docker push ghcr.io/<你的用户名>/greetlab:v0.1.0

# 拉下来跑一把，验证发布成功
docker run --rm ghcr.io/<你的用户名>/greetlab:v0.1.0 --name world
# 期望：Hello, world!
```

> [!warning] 两条坑
> - TestPyPI 只是"演练场"，**别传真 PyPI**（题目明确要求）；ghcr.io 的 token 需要开 `write:packages` 权限。
> - 镜像是平台相关的：`linux/amd64` 的镜像在 arm64（Apple Silicon）上要模拟器才能跑。

---

### B. 智能体编程（4 题）

#### B1. 用智能体浏览陌生代码库（理解 opencode 的安全机制）

题目给的现成例子就是 **opencode**（你现在用的这个工具本身），正好用它自问自答。给它一个明确的探索目标：

```text
请浏览当前仓库（opencode），解释它的安全 / 权限机制是怎么实现的：
1. 用户确认工具调用（权限请求）是在哪一步、怎么拦截的？
2. 有哪些权限模式（只读、自动批准等）？分别在哪个文件配置？
3. 沙箱 / 隔离是怎么做的？是否依赖容器或系统机制？
请给出关键源码文件路径和行号，不要贴大段代码。
```

> [!tip] 让智能体"读代码"而非"编答案"
> 要求它**给出文件:行号**，你就知道它真的去查了源码，而不是凭空编。若答错，把正确线索喂回去让它纠偏（就像带实习生）。

#### B2. 从零氛围编程一个小应用（不手写一行代码）

给智能体一个完整但宽松的目标，让它自己决定结构、写代码、编译、运行、修错。下面给一个 **C++** 例子（贴合你熟悉的语言）：

```text
用 C++ 写一个命令行小工具 csv2md：读取 CSV 文件，输出 Markdown 表格。
要求：
- 用 CMake 组织项目，生成单个可执行文件 csv2md；
- 正确处理引号包裹、含逗号的字段；
- 自己写 1 个样例 CSV，用 g++/cmake 编译并运行验证输出；
- 不要手把手告诉它怎么写，让它自己迭代直到能跑通。
```

```bash
# 验证命令（智能体跑通后你复核）
cmake -S . -B build && cmake --build build
./build/csv2md sample.csv
```

> [!note] 氛围编程（vibe coding）的精髓
> 你只描述"要什么"，实现、调试全交给智能体。但**验收归你**：跑一遍、看输出、挑边界（空字段、逗号字段）是否真的处理对了。

#### B3. AGENTS.md / Skill / 子智能体（何时用哪个）

> [!abstract]- 知识归档（三种上下文注入手段的区别）
> - **AGENTS.md**（opencode/Claude Code 等支持，Claude 用 `CLAUDE.md`）：启动时**全量**塞进上下文。放"每次都要遵守"的通用规则。
> - **Skill（技能）**：只注入名字+描述，智能体**按需加载**。放"偶尔用到的领域流程"，避免常驻撑爆上下文。
> - **子智能体（Subagent）**：独立上下文的专职小智能体，顶层调它，它只回结果。放"重活/研究"，不让中间过程污染主对话。
> - 选择原则：**每次都用的规则 → AGENTS.md；某类任务 → Skill；需要隔离上下文的批量工作 → 子智能体**。

```markdown
<!-- AGENTS.md（项目根目录，永远加载） -->
# 项目约定
- 改完 Python 文件后必须运行 `PYTHONPATH=src pytest` 并通过。
- 提交前用 `ruff check .`，不得提交未使用 import。
- 不要直接改 `pyproject.toml` 的 name/version 字段。
```

```markdown
<!-- .opencode/skills/代码检查/SKILL.md（按需加载的 Skill） -->
---
name: 代码检查
description: 对自上次 git 提交以来改动过的 Python 文件跑 mypy + ruff 类型检查与 lint。
---
# 流程
1. git diff --name-only 找出改动文件；
2. 对每个 .py 文件跑 mypy 与 ruff check；
3. 输出每处问题的文件:行号 + 修复建议，不改文件。
```

子智能体提示词（opencode 里建一个 agent）：

```text
一个"代码审查"子智能体：审查未提交的改动，重点关注正确性、边界条件和安全隐患，
输出按 Blocking / Suggestion / Nit 分级的中文评审意见，不改动任何文件。
```

> [!tip] 检验方法
> 故意写一行 `import os`（未使用），让智能体走一遍 AGENTS.md 的"ruff check"流程，看它是否真的遵守了规则；再让"代码审查"子智能体审一下，看它是否只给意见、不动文件。

#### B4. 用智能体完成 Markdown 无序列表正则替换（不直接编辑文件）

**目标**（来自代码质量一讲的练习5）：把 `code-quality.md` 里行首的 `- ` 无序列表标记替换成 `* `，但**不能**把日期（`2026-01-14`）、范围（`a-f`）、`-q` 选项这些位置也改了。

```bash
# 先取文件
curl -O https://raw.githubusercontent.com/missing-semester/missing-semester/refs/heads/master/_2026/code-quality.md
```

给智能体的提示（关键：**禁止直接编辑**）：

```text
把 code-quality.md 里行首的无序列表标记 `- ` 替换为 `* `。
约束：不要直接编辑文件；用第一讲提到的命令行工具（sed）加正则完成；
日期(2026-01-14)、范围(a-f)、选项(-q) 等其它位置的 `-` 必须保留。
完成后给出你用的 sed 命令，并贴 git diff 让我复核。
```

正确的 sed（锚定到"行首 + 可选空白 + `- `"）：

```bash
# -i 原地改；-E 用扩展正则；^ 锚定行首；[[:space:]]* 吸收缩进；\1 引用捕获组
sed -i -E 's/^([[:space:]]*)- /\1* /' code-quality.md

# 复核：只应改掉"行首的 - "，日期/范围/选项里的 - 不变
git diff code-quality.md
```

> [!warning] 为什么不能简单 `s/-/*/`
> 全局替换会把 `2026-01-14` 变成 `2026*01*14`、`[a-f]` 变成 `[a*f]`。必须锚定 `^`（行首）并限定"后面跟空格"，只命中真正的列表标记。

> [!tip] 智能体直接编辑文件 vs 用 sed 的取舍
> - 直接编辑：改动过程不可见、难审计、容易误伤、不易复现。
> - 用 sed：**命令即记录**，可放进脚本、可 diff、可回退、可复现——这正是题目让智能体改用命令行工具的用意。
> - 如果智能体还是去编辑文件，就明确说"不允许 write/edit 工具，只允许执行 shell 命令"。

---

### C. 不止于代码（3 题）

#### C1. 浏览知名项目源码，找出几类"值得写"的注释

选一个 C++ 项目（贴合你的语言）：`nlohmann/json` 或 `fmt`。用 `rg` 定位注释类型。

```bash
git clone https://github.com/nlohmann/json.git && cd json

# 有用的 TODO（带上下文的待办）
rg -n "TODO|FIXME|HACK" include/ src/

# 指向外部文档 / 论文 / issue 的引用
rg -n "https://|RFC|paper|issue #|C++ Standard|wg21|P[0-9]{4}" include/

# "为什么不用" / 血泪教训类（why not）
rg -n -i "we use .* instead|deliberately|intentionally|do not use|must not|otherwise.*break" include/
```

> [!tip] 找到后回答"少了会失去什么"
> - 少了 **why-not** 注释：后来人会"好心"把刻意避开的做法改回去，把东西搞坏。
> - 少了 **TODO 上下文**：只剩"TODO: optimize"这类废话，没人知道还缺什么、为何延期。
> - 少了 **外部引用**：实现与论文/规范的偏差无处可查，改错都无从对照。

#### C2. 好提交信息 vs 弱提交信息（体会补背景的费劲）

```bash
# 用你感兴趣的项目看最近提交（这里以 C++ 的 fmt 为例）
git clone https://github.com/fmtlib/fmt.git && cd fmt
git log --oneline -20
```

- **好提交**：标题即摘要，正文讲清"为什么改"（问题 → 解决方案 → 影响）。
- **弱提交**：只写"改了什么"，如 `fix bug`、`update code`、`fix crash`，看不出动机。

```bash
git show <弱提交的hash>      # 看它的 diff，试着推断"为什么"
```

> [!note] 按「问题 → 解决方案 → 影响」重写弱提交（示意）
> - 原：`fix crash`
> - 重写标题：`Fix use-after-free in parse when input is empty`
> - 重写正文：
>   - 问题：空输入时 `p` 在 `return` 后被继续引用，触发 UAF。
>   - 解决方案：提前校验 `input.empty()` 直接返回，避免后续解引用。
>   - 影响：崩溃修复；代价是对空输入多一次判断，可忽略。

> [!tip] 体悟
> 事后补背景极其费劲：你要反推当时的约束、翻代码、猜意图——而这些在写代码的当下本是一句话的事。所以**提交信息要在当下写**。

#### C3. 评估一个未解决的 issue

拿一个你使用的项目，找 `good first issue` / `help wanted` 标签的 issue 来评估（C++ 项目示例用 `nlohmann/json`）：

```bash
gh issue list --repo nlohmann/json --label "good first issue" --limit 10
gh issue view <number> --repo nlohmann/json
```

按讲义标准逐条打分：

| 标准 | 问什么 |
| --- | --- |
| 尊重维护者时间 | 有没有搜索过重复 issue？有没有贴最小复现？ |
| 环境 | 操作系统、版本号、相关配置写全了吗？ |
| 期望 vs 实际 | 说清"本该怎样"和"实际怎样"了吗？ |
| 复现步骤 | 具体到命令/操作，而不是"点了按钮"？ |
| 已尝试 | 是否说明自己做过什么排查？ |

> [!tip] 结论怎么写
> 例如：这个 issue 有环境、有复现命令、有最小示例，维护者可以直接开工 → 质量高；
> 那个只有一句"报错了"，缺环境与复现，维护者至少要来回追问 3 轮 → 质量低。

---

## 附录：知识点速查（第3周）

| 概念 | 命令 / 说明 |
| --- | --- |
| 建包目录 | `mkdir -p src/greetlab`，`__init__.py` 标记包 |
| 构建 wheel | `python -m build`（先 `pip install build`），产出 `dist/*.whl` |
| 建虚拟环境 | `python -m venv .venv` |
| 激活 venv | `source .venv/bin/activate`（Win：`.venv\Scripts\activate`） |
| 从 wheel 安装 | `pip install dist/*.whl` |
| 入口点 | `[project.scripts] sdt-greet = "greetlab.cli:main"` |
| argparse | `add_argument("--name", required=True)`；`parse_args()`；`p.error()` 退出码 2 |
| 跑测试 | `PYTHONPATH=src pytest` 或 `pip install -e . && pytest` |
| 断言 SystemExit | `pytest.raises(SystemExit)`，取 `e.value.code` |
| 看改动 | `git diff`；撤销无关改动 `git checkout -- <文件>` |
| PyTorch 训练三步 | `opt.zero_grad()` → `loss.backward()` → `opt.step()` |
| 评估模式 | `model.eval()` + `torch.no_grad()` |
| 固定随机种子 | `torch.manual_seed(20260907)` |

---

## 常用命令速查

| 命令 | 作用 | 常用参数 |
| --- | --- | --- |
| `python -m build` | 按 pyproject.toml 构建 | `--wheel` 只出 wheel、`--sdist` 只出源码包 |
| `python -m venv` | 建虚拟环境 | `<目录名>`，惯例 `.venv` |
| `pip install` | 装包 | `-e .` 可编辑、`*.whl` 从 wheel |
| `pytest` | 跑测试 | `-q` 精简、`-k 关键字` 筛选 |
| `git diff` | 看未暂存改动 | `--stat` 摘要、`-- <文件>` 只看某文件 |
| `git checkout --` | 丢弃工作区改动 | `<文件>` |
| `python -m pip install` | 以模块方式装 | `torch`（CPU）、`build` |
| `printenv` / `env` | 打印环境变量 | `> f` 存文件、`diff a b` 比较 |
| `uv lock` / `pip freeze` | 生成 lockfile | `uv.lock` / `requirements.txt` |
| `docker compose up` | 编排多容器 | `--build` 重建镜像、`-d` 后台 |
| `docker build` / `run` | 打镜像 / 跑容器 | `-t 名:tag`、`--rm` 用完即删 |
| `twine upload` | 发布到 PyPI | `-r testpypi` 上传测试源 |
| `sed -i -E 's/^([[:space:]]*)- /\1* /'` | 行首列表标记 `-` → `*` | `-i` 原地、`-E` 扩展正则、`^` 锚行首 |
| `rg -n "TODO\|FIXME"` | 搜注释/引用 | `-n` 行号、`-i` 忽略大小写 |
| `gh issue list` / `view` | 查 issue | `--label` 按标签筛 |

---

## 附录：Python 打包与 PyTorch 速查

### 1. 打包关键文件

```
q09/
├── pyproject.toml          # 构建配置 + 项目元数据 + 入口点
├── src/
│   └── greetlab/
│       ├── __init__.py     # 空文件，标记为包
│       └── cli.py          # 真正的代码（main 函数）
└── dist/                   # python -m build 的产物
    ├── greetlab_<学号>-0.1.0-py3-none-any.whl
    └── greetlab_<学号>-0.1.0.tar.gz
```

### 2. argparse 速查

| 写法 | 作用 |
| --- | --- |
| `p = argparse.ArgumentParser()` | 建解析器 |
| `p.add_argument("--name", required=True)` | 加必填参数 |
| `p.add_argument("--count", type=int, default=1)` | 加带类型/默认值的参数 |
| `a = p.parse_args()` | 解析，返回命名空间 |
| `a.name` | 取值 |
| `p.error("msg")` | 打印 usage+消息到 stderr，`exit(2)` |

### 3. PyTorch 核心对象

| Python/PyTorch | 说明 |
| --- | --- |
| `torch.tensor` | 张量，`torch.linspace(...).reshape(-1,1)` 造列向量 |
| `nn.Linear(1,1)` | 线性层，`model.weight` / `model.bias` 是参数 |
| `nn.MSELoss()` | 均方误差损失 |
| `torch.optim.SGD(model.parameters(), lr=0.1)` | SGD 优化器 |
| `loss.item()` | 把单元素张量转 Python 标量 |
| `model.eval()` / `torch.no_grad()` | 评估模式 / 关闭梯度 |
| `torch.manual_seed(n)` | 固定随机种子 |

### 4. 训练循环固定套路

```python
for _ in range(epochs):
    pred = model(x)          # 前向
    loss = loss_fn(pred, y)  # 算损失
    opt.zero_grad()          # 清梯度（关键！梯度默认累加）
    loss.backward()          # 反向传播
    opt.step()               # 更新参数
```
